# Copy to .env and adjust as needed. Do not commit real credentials.
#
# When DATABASE_URL is unset, the API uses an in-memory store (data is lost on
# restart). Set it to use PostgreSQL (see deploy/docker-compose.yml):
#
# DATABASE_URL=postgresql+psycopg://process:process@localhost:5432/procworks
